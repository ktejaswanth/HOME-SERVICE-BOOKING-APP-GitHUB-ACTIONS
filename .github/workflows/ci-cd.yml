name: Build & Push Frontend + Backend Images

on:
  push:
    branches:
      - main
      - master

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Backend Image
      uses: docker/build-push-action@v5
      with:
        context: ./Backend
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/home-service-backend:latest
          ${{ secrets.DOCKER_USERNAME }}/home-service-backend:${{ github.sha }}

    - name: Build and Push Frontend Image
      uses: docker/build-push-action@v5
      with:
        context: ./Frontend
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/home-service-frontend:latest
          ${{ secrets.DOCKER_USERNAME }}/home-service-frontend:${{ github.sha }}
